frontend:
  image:
    repository: gskplatacrn73d5y.azurecr.io/three-tier/frontend
    tag: sha-46fa90f
  replicaCount: 1
  greeting: Hello from PROD
  resources:
    requests:
      cpu: 30m
      memory: 32Mi
    limits:
      cpu: 100m
      memory: 128Mi
  useConfigMapHTML: false
  blueGreen:
    enabled: true
    # Prod requires manual promote — no auto-promotion even if analysis passes.
    # Operator must verify preview, then run `kubectl argo rollouts promote`.
    autoPromotionEnabled: false
    analysis:
      enabled: true
backend:
  image:
    repository: gskplatacrn73d5y.azurecr.io/three-tier/backend
    tag: sha-46fa90f
  replicaCount: 1
  echoMessage: backend response from prod
  resources:
    requests:
      cpu: 50m
      memory: 64Mi
    limits:
      cpu: 200m
      memory: 128Mi
  useEchoArgs: false
  env:
    APP_ENV: prod
    APP_VERSION: sha-46fa90f
  blueGreen:
    enabled: true
    autoPromotionEnabled: false
database:
  image:
    repository: postgres
    tag: 15-alpine
  database: appdb
  username: appuser
  password: prod-superSecret-change-me
  storage:
    size: 1Gi
    storageClass: managed-csi
  resources:
    requests:
      cpu: 100m
      memory: 256Mi
    limits:
      cpu: 250m
      memory: 512Mi
